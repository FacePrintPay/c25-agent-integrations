git clone https://github.com/Constellation25/Constellation25.git
# Unzip contents into the repo folder
git add .
git commit -m "Deploy AiMetaverse OSS registry and test workflows"
git push origin main
